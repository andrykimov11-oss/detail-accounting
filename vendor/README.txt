Офлайн-пакеты Python для Windows + Python 3.12 (win_amd64).
Ставятся так (это делает запусти.bat автоматически):
  .venv\Scripts\pip install --no-index --find-links vendor\wheels Flask openpyxl cryptography
Требуется Python 3.12 (не 3.14 — под неё этих сборок нет).
