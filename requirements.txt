# Ядро учёта работает на стандартной библиотеке Python 3.10+
# (xml.etree, sqlite3, hashlib, dataclasses) — внешних зависимостей не требует.

# Чтение выгрузок 1С (.xlsx) и обезличивание данных
openpyxl>=3.1,<4.0

# Веб-интерфейс оператора у станка (src/operator_app.py)
Flask>=3.0,<4.0

# Тесты
pytest>=8.0,<9.0
